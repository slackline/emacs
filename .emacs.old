;; Android Mode
(add-to-list 'load-path "~/.emacs.d/lisp/")
	    (require 'android-mode)
	    (setq android-mode-sdk-dir "~/Dropbox/personal/android")
	    (add-hook 'gud-mode-hook
            (lambda ()
            (add-to-list 'gud-jdb-classpath "/home/gregj/work/android-sdk-linux_86/platforms/android-7/android.jar")
            ))

;; Enable automatic file header's with defaults then set various
   ;; parameters
 (load "~/.emacs.d/lisp/filehdr.el" t t t)
   ;; Name 
 (setq file-header-user-author "Neil Shephard")
   ;; Address
 (setq file-header-user-address
   "                                                                                                                                                                                                                                                                            
 ")
   ;; Telephone
 (setq file-header-user-telephone "")
   ;; FAX
 (setq file-header-user-FAX "")
   ;; Email
 (setq file-header-user-email "nshephard@gmail.com")
   ;; URL
 (setq file-header-user-URL "http://kimura.no-ip.org/")
   ;; Licence
 (setq file-header-user-license "GNU GPL")

 ;;; Key bindings
 (local-set-key "C-tab C-f" 'make-file-header)
 (local-set-key "C-tab C-d" 'update-date-and-minor-version)
 
(local-set-key "" 'update-file-header-and-save)
(local-set-key "" 'update-simple-checksum)
   ;; Standard comment prefixes
 (setq file-header-standard-comment-prefixes
              (append file-header-standard-comment-prefixes
              (list (list "" "## "))))
