;; MPDEL CONFIGURATION
;; --------------------------------------
(require 'mpdel)
(mpdel-mode)
(require 'ivy-mpdel)

;; Alarmpi control
(setq libmpdel-hostname "192.168.1.21")
(setq libmpdel-port 6600)
